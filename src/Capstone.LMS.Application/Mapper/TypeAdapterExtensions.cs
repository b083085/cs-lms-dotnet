using Mapster;

namespace Capstone.LMS.Application.Mapper
{
    public static class TypeAdapterExtensions
    {
        public static TypeAdapterConfig ConfigureMappings(this TypeAdapterConfig config)
        {
            return config;
        }
    }
}
